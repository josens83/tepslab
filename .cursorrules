# TepsLab Project Rules

## 프로젝트 개요
TEPS 시험 준비를 위한 프리미엄 온라인 학습 플랫폼
- Framework: React 19 + TypeScript + Vite 7
- Styling: Tailwind CSS
- Animation: Framer Motion
- Backend: Supabase Direct

## 디자인 시스템 규칙

### 컴포넌트 사용 (필수)
기존 컴포넌트를 반드시 사용하세요. 새로 만들지 마세요:

```tsx
// ✅ 올바른 import
import { Button, Card, Input, Modal, Toast, LoadingSpinner } from '@/components/common';

// ❌ 금지 - 직접 구현
<button className="...">  // Button 컴포넌트 사용
<div className="card-like"> // Card 컴포넌트 사용
```

### 브랜드 색상
Tailwind 클래스로만 사용:
- `bg-brand-yellow`, `text-brand-yellow` (#FFC600)
- `bg-brand-cyan`, `text-brand-cyan` (#00D9FF)
- `bg-brand-purple`, `text-brand-purple` (#9945FF)
- `bg-brand-pink`, `text-brand-pink` (#E91E63)
- `bg-brand-green`, `text-brand-green` (#4CAF50)

### 버튼 스타일
```tsx
<button className="btn-primary btn-yellow">노란 버튼</button>
<button className="btn-primary btn-purple">보라 버튼</button>
<button className="btn-primary btn-cyan">청록 버튼</button>
```

### 카드 스타일
```tsx
<div className="card">카드 내용</div>
```

## 반응형 디자인 (Mobile-First 철칙)

### 브레이크포인트
```
Mobile: default (< 640px) - 항상 먼저 작성
Tablet: sm (640px), md (768px)
Desktop: lg (1024px), xl (1280px)
```

### Mobile-First 규칙
```tsx
// ✅ 올바른 순서 (모바일 → 데스크톱)
className="w-full sm:w-1/2 lg:w-1/3"
className="text-sm md:text-base lg:text-lg"
className="flex flex-col md:flex-row"
className="p-4 md:p-6 lg:p-8"

// ❌ 잘못된 순서
className="w-1/3 lg:w-full"
```

### 터치 타겟 (최소 44x44px)
```tsx
// ✅ 충분한 터치 영역
className="min-h-[44px] min-w-[44px] p-3"

// ❌ 터치하기 어려움
className="p-1 text-xs"
```

### 반응형 그리드
```tsx
// 카드 그리드 (1→2→3열)
<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
  {items.map(item => <Card key={item.id} />)}
</div>
```

### 반응형 타이포그래피
```tsx
<h1 className="text-2xl md:text-3xl lg:text-4xl font-bold">제목</h1>
<p className="text-sm md:text-base leading-relaxed">본문</p>
```

### 모바일 네비게이션
```tsx
<nav className="hidden md:flex gap-4">데스크톱 메뉴</nav>
<button className="md:hidden" aria-label="메뉴 열기">햄버거</button>
```

## UI 상태 구현 (4가지 필수)

모든 데이터 페칭 컴포넌트에 4가지 상태 구현 필수:

### 1. 로딩 상태
```tsx
// Skeleton UI: 레이아웃 예측 가능 시 (리스트, 카드)
<div className="animate-pulse space-y-4">
  <div className="h-4 bg-gray-200 rounded w-3/4"></div>
  <div className="h-32 bg-gray-200 rounded"></div>
</div>

// Spinner: 전체 페이지, 버튼 내부
if (isLoading) return <LoadingSpinner />;

// 버튼 내 로딩
<Button disabled={isSubmitting}>
  {isSubmitting ? <LoadingSpinner size="sm" /> : '제출'}
</Button>
```

### 2. 에러 상태
```tsx
if (error) return (
  <div className="text-center py-12">
    <div className="text-red-500 text-5xl mb-4">⚠️</div>
    <p className="text-gray-600 mb-4">{error.message}</p>
    <Button onClick={refetch} variant="outline">다시 시도</Button>
  </div>
);
```

### 3. 빈 상태
```tsx
if (data.length === 0) return (
  <div className="text-center py-12">
    <div className="text-5xl mb-4">📭</div>
    <h3 className="text-lg font-semibold mb-2">데이터가 없습니다</h3>
    <p className="text-gray-500 mb-4">첫 번째 항목을 추가해보세요</p>
    <Button onClick={onCreate}>새로 만들기</Button>
  </div>
);
```

### 4. 성공 피드백
```tsx
import { toast } from '@/components/common/Toast';

// 성공/에러/정보
toast.success('저장되었습니다');
toast.error('저장에 실패했습니다');
toast.info('자동 저장됨');
```

### 전체 패턴
```tsx
const DataList = () => {
  const { data, isLoading, error, refetch } = useQuery();

  if (isLoading) return <ListSkeleton count={5} />;
  if (error) return <ErrorState onRetry={refetch} />;
  if (!data?.length) return <EmptyState action={handleCreate} />;

  return <ul>{data.map(item => <li key={item.id}>{item.title}</li>)}</ul>;
};
```

## 접근성 (WCAG 2.1 AA)

### 시맨틱 HTML
```tsx
<header /><nav /><main /><footer />  // ✅
<div class="header" />               // ❌
```

### 키보드 접근성
```tsx
// ✅ 포커스 표시
className="focus-visible:ring-2 focus-visible:ring-primary"

// ❌ 포커스 제거 금지
className="outline-none focus:outline-none"
```

### 이미지/아이콘
```tsx
// 의미 있는 이미지
<img src="..." alt="2024년 매출 그래프" />

// 장식 이미지
<img src="..." alt="" aria-hidden="true" />

// 아이콘 버튼
<button aria-label="검색"><SearchIcon aria-hidden="true" /></button>
```

### 폼 접근성
```tsx
<label htmlFor="email">이메일</label>
<input id="email" aria-describedby="email-error" aria-invalid={!!error} />
{error && <p id="email-error" role="alert">{error}</p>}
```

### 색상 대비
```
일반 텍스트: 4.5:1 이상
큰 텍스트: 3:1 이상
❌ 색상만으로 정보 전달 금지
✅ 색상 + 아이콘 + 텍스트
```

## 금지 사항
- 하드코딩된 색상: `bg-blue-500`, `text-gray-800` 등
- 인라인 스타일: `style={{ color: 'red' }}`
- 기존 컴포넌트 있는데 새로 구현
- label 없는 input/select
- alt 없는 img
- 텍스트 없는 버튼 (aria-label 필수)

## 애니메이션 (Framer Motion)

### Duration 기준
```
50-100ms   호버, 색상 변화 (즉각적)
150-200ms  드롭다운, 툴팁 (빠름)
200-300ms  모달, 카드 확장 (표준) ← 기본값
300-500ms  페이지 전환, 성공 표시 (강조)
```

### Easing 기준
```
ease-out     등장 (빠르게 시작 → 천천히 끝)
ease-in      퇴장 (천천히 시작 → 빠르게 끝)
spring       탄성 효과 (버튼, 드래그)
```

### 기본 패턴
```tsx
import { motion, AnimatePresence } from 'framer-motion';

// 버튼 인터랙션
<motion.button
  whileHover={{ scale: 1.05 }}
  whileTap={{ scale: 0.95 }}
  transition={{ type: 'spring', stiffness: 400, damping: 17 }}
/>

// 등장 애니메이션
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.3, ease: 'easeOut' }}
/>

// 모달 (퇴장 포함)
<AnimatePresence>
  {isOpen && (
    <motion.div
      initial={{ opacity: 0, scale: 0.95 }}
      animate={{ opacity: 1, scale: 1 }}
      exit={{ opacity: 0, scale: 0.95 }}
    />
  )}
</AnimatePresence>
```

### 성능 최적화
```tsx
// ✅ GPU 가속 (빠름): transform, opacity
animate={{ opacity: 1, x: 0, scale: 1 }}

// ❌ 레이아웃 유발 (느림): width, height, top
animate={{ width: 100, height: 100 }}
```

### prefers-reduced-motion 지원
```tsx
const shouldReduceMotion = useReducedMotion();
transition={{ duration: shouldReduceMotion ? 0 : 0.3 }}
```

## UI 품질 테스트

### 도구 체인
```
Playwright    시각적 회귀 테스트 (스크린샷 비교)
axe-core      접근성 자동 테스트 (WCAG 검사)
Lighthouse    성능/접근성/SEO 점수
jsx-a11y      코드 작성 시점 접근성 린트
```

### 시각적 테스트
```typescript
await expect(page).toHaveScreenshot('homepage.png');
```

### 접근성 테스트
```typescript
const results = await new AxeBuilder({ page }).analyze();
expect(results.violations).toEqual([]);
```

## 성능 최적화 (Core Web Vitals)

### 목표
```
LCP ≤ 2.5초  |  INP ≤ 200ms  |  CLS ≤ 0.1
```

### LCP 최적화
```tsx
// ✅ 히어로 이미지에 priority
<Image src="/hero.jpg" priority />

// ❌ LCP 이미지 lazy loading
<Image src="/hero.jpg" />
```

### CLS 최적화
```tsx
// ✅ 이미지 크기 명시
<Image src="/photo.jpg" width={800} height={600} />

// ✅ aspect-ratio 사용
<div className="aspect-video"><Image fill /></div>

// ❌ 크기 없는 이미지
<img src="/photo.jpg" />
```

### INP 최적화
```tsx
// 무거운 컴포넌트 동적 임포트
const Heavy = dynamic(() => import('./Heavy'), { ssr: false });

// useTransition으로 비긴급 업데이트 분리
startTransition(() => setResults(filtered));
```

## AI 프롬프트 가이드

### 프롬프트 5요소
```
1. 역할      → "React + Tailwind 전문 개발자로서"
2. 컨텍스트   → "기존 디자인 시스템 사용, 모바일 우선"
3. 작업      → "로그인 폼 컴포넌트 만들어줘"
4. 요구사항   → "이메일/비밀번호/제출버튼 포함"
5. 출력형식   → "TypeScript + interface 포함"
```

### 단계별 요청
```
1단계: 구조 → "레이아웃, 컴포넌트 배치만"
2단계: 기능 → "로직, 상태관리, 검증 추가"
3단계: 스타일 → "디자인 시스템 색상, 애니메이션"
```

### v0.dev 워크플로우
```
1. v0.dev에서 프로토타입 생성
2. 코드 복사 후 붙여넣기
3. 기존 컴포넌트로 교체 (Button, Card 등)
4. 색상 → brand-* 클래스로 교체
5. 접근성 검수 (aria, label)
```

## 디자이너 없이 작업하기

### Copy, Don't Create
```
❌ "이 화면 디자인해줘"
✅ "Linear.app의 목록 레이아웃 참고해서"

레퍼런스:
- SaaS: Linear, Notion, Stripe
- 마케팅: Apple, Airbnb
- 개발자: Vercel, Supabase
```

### 디자인 시스템 체크리스트
```
□ 색상: brand-* 클래스
□ 타이포: text-sm/base/lg/xl
□ 간격: p-4, gap-4, space-y-4
□ 컴포넌트: Button, Card, Input 등
□ 반응형: sm/md/lg/xl
□ 접근성: WCAG 2.1 AA
```

### 최소 노력 UX 개선
```
1. 로딩 상태 추가
2. 에러 메시지 친절하게
3. 빈 상태에 CTA 버튼
4. 성공 피드백 즉시 표시
5. 터치 영역 44x44px
```

## TypeScript
- strict mode 필수
- any 타입 금지
- 모든 props에 interface 정의

## 파일 구조
- 페이지: `src/pages/`
- 컴포넌트: `src/components/`
- 훅: `src/hooks/`
- 타입: `src/types/`
- 유틸: `src/lib/`
