#!/usr/bin/env sh

# 배포 전 검증 자동화 (챕터 6)
# TypeScript 타입 체크 및 ESLint 검증

echo "🔍 Pre-commit 검증 시작..."

# 전체 TypeScript 타입 체크
echo "📝 TypeScript 타입 체크..."
cd client && npm run typecheck

if [ $? -ne 0 ]; then
  echo "❌ TypeScript 오류가 있습니다. 커밋이 중단됩니다."
  exit 1
fi

echo "✅ 검증 완료!"
