#!/usr/bin/env sh

# 배포 전 검증 자동화 (챕터 6)
# lint-staged로 스테이징된 파일만 검증

echo "🔍 Pre-commit 검증 시작..."

# lint-staged 실행 (스테이징된 파일만 검증)
npx lint-staged

if [ $? -ne 0 ]; then
  echo "❌ 검증 실패. 커밋이 중단됩니다."
  exit 1
fi

echo "✅ 검증 완료!"
